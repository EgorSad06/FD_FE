<ResourceDictionary
    x:Name="ResDict"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ControlTemplate TargetType="Button" x:Key="T_std_but">
        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="16" Tag="{TemplateBinding Foreground}">
            <TextBlock x:Name="textblock" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{TemplateBinding FontFamily}"
            Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" Tag="{TemplateBinding Background}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="textblock" Property="Foreground" Value="{Binding ElementName=textblock, Path=Tag}"/>
                <Setter TargetName="border" Property="Background" Value="{Binding ElementName=border, Path=Tag}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.6"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    
    <!-- Стандартные стили (рекомендованно в BasedOn для др стилей) -->
    <FontFamily x:Key="FF_main">Bahnschrift</FontFamily>

    <Style x:Key="S_std_tblk" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="{StaticResource FF_main}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="TextAlignment" Value="Center"/>
    </Style>

    <Style x:Key="S_std_tbox" TargetType="TextBox">
        <Setter Property="BorderThickness" Value="4"/>
        <Setter Property="BorderBrush" Value="White"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="Black"/>
        <Setter Property="FontFamily" Value="{StaticResource FF_main}"/>
        <Setter Property="FontSize" Value="24"/>
    </Style>

    <Style x:Key="S_std_rbut" TargetType="RadioButton">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontFamily" Value="{StaticResource FF_main}"/>
        <Setter Property="FontSize" Value="24"/>
    </Style>
    
    <Style x:Key="S_std_but" TargetType="Button">
        <Setter Property="Width" Value="160"/>
        <Setter Property="Height" Value="48"/>
        <Setter Property="Template" Value="{StaticResource T_std_but}"/>
        <Setter Property="Background" Value="WhiteSmoke"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontFamily" Value="{StaticResource FF_main}"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="Focusable" Value="False"/>
    </Style>

    
    <ControlTemplate x:Key="T_card_but" TargetType="Button">
        <Border x:Name="border"
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}">
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="BorderBrush" Value="White"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>